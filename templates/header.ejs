<header class="header">
    <nav class="header-nav">
        <a class="logo" href="/">GamePedia</a>
        <form class="game-list" method="get" action="/games/list">
            <button type="submit" class="btn btn-game-list">Список всіх ігор</button>
        </form>
        <form class="search" method="get" action="/games/list">
            <input type="text" name="title" placeholder="Пошук гри...">
            <button type="submit" class="btn">Знайти</button>
        </form>
        <div class="buttons">
            <% if (user) { %>
                <% if (user.isAdmin) { %>
                <a href="/games/add" class="btn btn-add-game">Додати гру</a>
                <% } %>
                <a href="/profile">Мій кабінет</a>
            <% } else { %>
                <a href="/login">Увійти</a>
                <a href="/registration">Зареєструватись</a>
            <% } %>
        </div>
    </nav>
</header>

